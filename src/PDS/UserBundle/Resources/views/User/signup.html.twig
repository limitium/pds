{% extends '::base.html.twig' %}

{% block content %}

<div class="row">
    <div class="span4 offset4">

        <form  action="{{ path("usernew") }}" method="post" class="well" {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Sign up</button>
            </div>
        </form>
    </div>
</div>
        {% endblock%}